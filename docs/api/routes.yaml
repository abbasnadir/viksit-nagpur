openapi: 3.0.3
info:
  title: Nagpur Traffic AI Backend API
  description: Production-grade API routes. Unimplemented routes removed.
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check endpoint for container probes.
      tags:
        - Health
      responses:
        '200':
          description: OK

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
